// Minimal type stubs to satisfy any existing imports of supabase types.
// All public-facing types are intentionally permissive in the FastAPI build.
export type Json = any;
export type Database = any;
